{
  "name": "hops-prerelease-tests",
  "private": true,
  "type": "module",
  "description": "End-to-end and contract tests that run against a fresh HOPS instance before release",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "show-report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}

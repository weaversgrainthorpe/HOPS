{
  "name": "hops-demo-record",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-shot script that records a HOPS demo video via headless Chromium",
  "scripts": {
    "record": "node record.mjs",
    "convert": "node convert.mjs"
  },
  "dependencies": {
    "playwright": "^1.50.0",
    "ffmpeg-static": "^5.2.0"
  }
}
